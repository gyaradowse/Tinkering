public class Solution {
    public bool IsValid(string s) {
        var split = s.Split();
        foreach (var string in split)
        {
            switch (string)
            {
                
                default:
            }
        }
    }
}